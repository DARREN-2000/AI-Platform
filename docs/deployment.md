# Deployment

## Production Requirements

For a production deployment, we strongly recommend a Kubernetes cluster with GPU support.

## Kubernetes Setup

(Helm charts are currently in development).

## GitHub Pages Deployment

The frontend dashboard is automatically deployed to GitHub Pages via the `.github/workflows/deploy-pages.yml` workflow when changes are merged to the `main` branch.
