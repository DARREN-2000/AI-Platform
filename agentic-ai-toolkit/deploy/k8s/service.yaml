apiVersion: v1
kind: Service
metadata:
  name: agentic-toolkit
  namespace: agentic-toolkit
  labels:
    app: agentic-toolkit
spec:
  type: ClusterIP
  selector:
    app: agentic-toolkit
  ports:
    - name: http
      port: 80
      targetPort: 8000
