[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "intentgraph"
version = "0.1.0"
description = "Agentic dependency graph orchestration"
authors = ["Enterprise AI Platform <team@example.com>"]

[tool.poetry.dependencies]
python = "^3.12"
pydantic = "^2.0"
networkx = "^3.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"
